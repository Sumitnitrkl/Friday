# FRIDAY Requirements
# Install with: pip install -r requirements.txt

# Core
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0        # loads GEMINI_API_KEY from a .env file

# Brain — Google Gemini with function calling (free)
google-genai>=1.0.0

# Holographic HUD desktop app (python main.py --ui)
websockets>=12.0
pywebview>=5.0           # native desktop window (falls back to Edge/Chrome app-mode)

# Speech-to-Text (SpeechRecognition + Google's free recognizer; needs internet)
SpeechRecognition>=3.10
pyaudio>=0.2.13
# macOS: brew install portaudio then pip install pyaudio
# Windows: pip install pipwin && pipwin install pyaudio

# Text-to-Speech
edge-tts>=6.1.9          # online Microsoft Neural voices (natural)
pyttsx3>=2.90            # offline fallback
pygame>=2.5.0            # plays the neural-voice audio (no ffmpeg needed)

# Skills / tools
psutil>=5.9              # process mgmt + battery/CPU status
pyautogui>=0.9.54        # media keys, screenshots, typing
ddgs>=6.0.0              # free web search (text results)

# Voice ID — only obey the enrolled user (lightweight, offline)
numpy>=1.24
scipy>=1.10
python_speech_features>=0.6

# Windows-only (install only on Windows)
# pycaw>=20230407        # Windows volume control: pip install pycaw
# comtypes               # required by pycaw
# wmi>=1.5.1             # Windows WMI for brightness

# macOS-only (install via brew, not pip)
# brew install blueutil      → bluetooth control
# brew install brightness    → display brightness

# Optional: nircmd on Windows for volume fallback
